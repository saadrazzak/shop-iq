// Values that are stable across retailer / platform changes stay here as
// compile-time constants. Values that may need updating when a site changes
// (API endpoints, DOM selectors, query params, credentials) live in
// src/config/default-config.json and are read via getConfig().
